package box.kotor.item;

enum CraftType {
    ;
    
    public boolean isWorkbench() {
        switch (this) {
            default: throw new IllegalStateException();
        }
    }
    
    public boolean isMedbench() {
        switch (this) {
            default: throw new IllegalStateException();
        }
    }
}
